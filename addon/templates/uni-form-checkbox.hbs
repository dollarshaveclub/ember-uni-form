
{{#if label }}
  <span class="label">{{ label }}</span>
{{/if}}

{{ input
  autofocus=autofocus
  checked=checked
  disabled=disabled
  name=name
  required=required
  type='checkbox'
}}

{{ yield }}
