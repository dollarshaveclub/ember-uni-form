
{{#if label }}
  <span class="label">{{{ statusText }}}</span>
{{/if}}

{{ uni-form-radio-tag
  autofocus=autofocus
  disabled=disabled
  groupValue=groupValue
  name=name
  type='radio'
  value=value
}}

{{ yield }}
