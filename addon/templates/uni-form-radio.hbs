
{{#if label }}<span class="label">{{ label }}</span>{{/if}}

{{ uni-form-radio-input
  disabled=disabled
  groupValue=groupValue
  name=name
  required=required
  type='radio'
  value=value
}}

{{ yield }}
