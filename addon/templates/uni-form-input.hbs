
{{ partial 'uni-form-label-and-message' }}

{{ input
  autofocus=autofocus
  disabled=disabled
  maxlength=maxlength
  name=name
  pattern=pattern
  placeholder=placeholder
  type=type
  value=value
}}

{{ yield }}
