
{{#if label }}
  <span class="label">{{{ statusText }}}</span>
{{/if}}

{{ input
  autofocus=autofocus
  disabled=disabled
  maxlength=maxlength
  name=name
  pattern=pattern
  placeholder=placeholder
  type=type
  value=value
}}

{{ yield }}
