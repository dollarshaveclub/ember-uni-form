import { prettyPath } from '../../../helpers/pretty-path';
import { module, test } from 'qunit';

module('helper:pretty-path');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = prettyPath([ '42' ]);
  assert.ok(result);
});
